export const STAFF_DASHBOARD_SECTIONS = Object.freeze({
  dashboard: true,
  tasks: true,
  requests: false
});

export const STAFF_REQUESTS_SECTIONS = Object.freeze({
  requests: true
});

export const STAFF_NOTIFICATIONS_SECTIONS = Object.freeze({
  notifications: true
});

export const STAFF_SCHEDULE_SECTIONS = Object.freeze({
  schedule: true
});

export const STAFF_HISTORY_SECTIONS = Object.freeze({
  history: true
});

export const STAFF_SETTINGS_SECTIONS = Object.freeze({
  settings: true
});
